var side1 = 7;
var side2 = 8;
var side3 = 9;
var area;
var semiP;

/*we have to find the semi-perimeter first 
because we don't have the height of triangle */

function semiPerimeter() {

((side1+side2+side3)/2);

};

function SP(){

semiPerimeter((semiP-side1)*(semiP-side2)*(semiP-side3))

}

area = math.sqrt(SP());

console.log("The area of the triangle is " + area);